@using YogaStudio.App.Models.ViewModels
@model ICollection<OrderPackageViewModel>

@{
    ViewBag.Title = "Packages in the order";
}
<div class="container">
    <h1>@ViewBag.Title</h1>
    <table class="table table-striped table-hover ">
        <thead>
            <tr>
                <th>Name</th>
                <th>Quantity</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var entity in Model)
            {
                <tr>
                    <td>@entity.Package.Name</td>
                    <td>@entity.Units</td>
                    <td>$@entity.Package.Price</td>
                </tr>
            }
        </tbody>
    </table>
    <br>
    @Html.ActionLink("Back to orders", "Index", "Orders", routeValues: new { area = "Admin" }, htmlAttributes: new { @class = "btn btn-danger" })
    <br>
</div>
